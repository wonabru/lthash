package main

import lthash "./libs"

func main() {

	h := lthash.New16()

	h.Add([]byte("Hello"))
	s1 := h.Sum(nil)
	h.Add([]byte("World"))
	s2 := h.Sum(nil)
	h.Remove([]byte("World"))
	s3 := h.Sum(nil)

	println(s1)
	println(s2)
	println(s3)

}
